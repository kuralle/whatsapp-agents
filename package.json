{
  "name": "kuralle-whatsapp-agents",
  "private": true,
  "type": "module",
  "description": "Deep, runnable WhatsApp agent examples built on Kuralle.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "booking": "bun run booking/run.ts",
    "pharmacy": "bun run pharmacy/run.ts",
    "clothing": "bun run clothing/run.ts"
  },
  "dependencies": {
    "@ai-sdk/google": "^3.0.0",
    "@ai-sdk/openai": "^3.0.0",
    "@ai-sdk/xai": "^3.0.0",
    "@kuralle-agents/core": "^0.6.0",
    "@kuralle-agents/engagement": "^0.6.0",
    "@kuralle-agents/messaging": "^0.6.0",
    "@kuralle-agents/messaging-meta": "^0.6.0",
    "ai": "^6.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "bun-types": "latest",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
